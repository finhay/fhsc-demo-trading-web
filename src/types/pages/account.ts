export type InfoRowProps = {
    label: string;
    value?: string | null;
};

export type EnterpriseContract = {
    contract_type: string;
    pathFile: string;
    owner: string;
    title: string;
};
