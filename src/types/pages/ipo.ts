export type IpoTimeLeft = {
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
};

export type IpoRegistrationStatusConfig = {
    badgeClass: string;
    messageKey: string;
};

export type IpoInfoSectionItem = {
    label: string;
    value: string;
};
