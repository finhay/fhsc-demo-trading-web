export type ResetPasswordResponse = {
    error_code: string;
    message: string;
    result?: any;
};

export type ChangePasswordResponse = {
    error_code: string;
    message: string;
    result?: any;
};
